[pytest]
pythonpath = .
testpaths = tests
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
markers =
    unit: Fast isolated unit tests
    integration: Integration tests with storage or database
    api: FastAPI endpoint tests
    e2e: End-to-end full workflow tests
    evals: RAG evaluation tests

